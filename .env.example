# URL base do painel admin (projeto separado "adom-admin"), que expõe
# /api/products e /api/categories. Em produção, aponte para a URL do
# admin publicado no Vercel.
VITE_ADMIN_API_URL=http://localhost:3000
